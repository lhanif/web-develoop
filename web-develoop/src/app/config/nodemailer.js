// const nodemailer = require("nodemailer");
// require("dotenv").config(); // Memastikan .env dibaca

// const transporter = nodemailer.createTransport({
//   host: "smtp.gmail.com",
//   port: 587, // Gunakan 465 untuk SSL
//   secure: false, // false untuk TLS, true untuk SSL
//   auth: {
//     user: process.env.EMAIL_USER, // Pastikan variabel environment sudah diisi dengan email Anda
//     pass: process.env.EMAIL_PASS, // Pastikan variabel environment sudah diisi dengan app password Gmail Anda
//   },
// });

// module.exports = transporter;
